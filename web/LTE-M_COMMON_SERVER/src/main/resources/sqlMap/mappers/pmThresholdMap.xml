<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ltem.dao.PmThresholdDAO">
	
	<update id="thresholdMod" parameterType="java.util.HashMap">
		${sql}
	</update>
	<select id = "thresholdCheck" parameterType="java.util.HashMap" resultType="java.lang.Integer">
		${sql}
	</select>
	<select id = "getSearchData" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		${sql}
	</select>
</mapper>